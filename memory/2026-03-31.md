# 2026-03-31 - Vitest 迁移工作继续

## 今日任务
- 继续执行 `yarn run test:vi` 并修复相关错误
- 修复多个测试文件以适配 Vitest

## 已修复的测试文件

### 1. cache.spec.ts (packages/cache/src/)
- 修复了 localStorage mock 的问题，将 `data` 变量提升到 describe 作用域
- 修复了 `expect(meta).not.toBeNaN()` 为 `expect(meta).toBeTruthy()`
- 将 notify 相关测试改为 async/await 形式
- 添加了 `vi.restoreAllMocks()` 来重置 mock

### 2. 货币管道测试
- **mega.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone
- **price.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone
- **cny.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone

### 3. 其他管道测试
- **mask.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone
- **filter.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone
- **url.pipe.spec.ts**: 使用 fakeAsync/tick，标记组件为 standalone

### 4. ACL 指令测试
- **acl-if.directive.spec.ts**: 
  - 移除了 ComponentFixtureAutoDetect
  - 使用 fakeAsync/tick
  - 标记测试组件为 standalone
- **acl.directive.spec.ts**:
  - 移除了 ComponentFixtureAutoDetect
  - 使用 fakeAsync/tick
  - 标记测试组件为 standalone

## 关键修复模式
1. **ComponentFixtureAutoDetect 问题**: 在 Vitest 中这个提供者可能导致问题，已移除并改用显式的 fixture.detectChanges()
2. **Standalone 组件**: 所有测试组件都标记为 standalone: true，并在 TestBed.configureTestingModule 中通过 imports 引入
3. **fakeAsync/tick**: 大量使用 fakeAsync 和 tick 来确保 Angular 变更检测正确执行
4. **Async/Await**: 对于 Observable 相关测试，使用 firstValueFrom 替代 done() 回调

## Git 提交记录
- `9ecbb904f`: fix: use void operator for intentionally unused promise
- `d0cfa9b24`: fix: use underscore prefix for unused promise variable
- `59b6d02f6`: fix: unused variable in cache spec
- `a54ed3a36`: fix: fix acl directive tests - use fakeAsync and standalone
- `eca72817c`: fix: continue fixing vitest tests - currency, filter, url, mask pipes
- `68e530f18`: fix: continue fixing vitest test issues

## 测试结果
✅ 编译通过！
- 57 个测试文件通过，96 个失败
- 786 个测试用例通过，117 个失败

## 关键发现
1. 主要失败原因：
   - Locale 数据问题：需要注册更多的 locale 数据
   - Angular 变更检测问题：需要正确使用 fakeAsync/tick
   - Standalone 组件配置问题

2. 已成功修复的模式：
   - 使用 fakeAsync/tick 替代 ComponentFixtureAutoDetect
   - 使用 async/await + firstValueFrom 替代 done() 回调
   - 标记测试组件为 standalone: true
   - 使用 void 操作符处理故意不使用的 Promise

## 待继续
还需要继续修复剩余的 96 个测试文件和 117 个失败测试。
