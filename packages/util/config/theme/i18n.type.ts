export interface AlainThemeI18nConfig {
  /**
   * Overrides the default interpolation start and end delimiters ({{ and }}).
   *
   * 改写默认的插值表达式起止分界符（{{ 和 }}）。
   */
  interpolation?: [string, string];
}
